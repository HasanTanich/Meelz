import { FilterMasterClassPipe } from './filter-master-class.pipe';

describe('FilterMasterClassPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMasterClassPipe();
    expect(pipe).toBeTruthy();
  });
});
